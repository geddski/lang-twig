# Mixed Twig Comment in script

<script>
  /foo/
  {% comment %}
    yolo
  {% endcomment %}
</script>

==>

Template(
  Element(OpenTag(StartTag,TagName,EndTag),
  Script(ExpressionStatement(RegExp)),
  Comment,
  CloseTag(StartCloseTag,TagName,EndTag)),
)
